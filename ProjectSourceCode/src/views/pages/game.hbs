<div class="row">
    <div class="col-7">
        <div class="row game-info">
            <div id="points" class="col-1">Points</div>
            <div id="category" class="col-5">Category</div>
            <div id="user-score" class="col-2">Score
                <div id="strokes">Strokes</div>
            </div>
        </div>
        <div id="actual-game" class="row">
            <div class="row">
                <div id="clearbtn"></div>
                <br>
                <br>
                <br>
                <div id="guess"></div>
                <br>
                <br>
                <br>
            </div>
        </div>
        <div style="border-style: dashed none none none; border-width: 2px; position:absolute; width:65%"></div>
        <div id="buttonContainer" class="row">
            <div class="keyboard_row">
                <button id="q" type="button" class="game btn btn-outline-secondary btn-lg" value="q">q</button>
                <button id="w" type="button" class="game btn btn-outline-secondary btn-lg" value="w">w</button>
                <button id="e" type="button" class="game btn btn-outline-secondary btn-lg" value="e">e</button>
                <button id="r" type="button" class="game btn btn-outline-secondary btn-lg" value="r">r</button>
                <button id="t" type="button" class="game btn btn-outline-secondary btn-lg" value="t">t</button>
                <button id="y" type="button" class="game btn btn-outline-secondary btn-lg" value="y">y</button>
                <button id="u" type="button" class="game btn btn-outline-secondary btn-lg" value="u">u</button>
                <button id="i" type="button" class="game btn btn-outline-secondary btn-lg" value="i">i</button>
                <button id="o" type="button" class="game btn btn-outline-secondary btn-lg" value="o">o</button>
                <button id="p" type="button" class="game btn btn-outline-secondary btn-lg" value="p">p</button>
            </div>
            <div class="keyboard_row">
                <button id="a" type="button" class="game btn btn-outline-secondary btn-lg" value="a">a</button>
                <button id="s" type="button" class="game btn btn-outline-secondary btn-lg" value="s">s</button>
                <button id="d" type="button" class="game btn btn-outline-secondary btn-lg" value="d">d</button>
                <button id="f" type="button" class="game btn btn-outline-secondary btn-lg" value="f">f</button>
                <button id="g" type="button" class="game btn btn-outline-secondary btn-lg" value="g">g</button>
                <button id="h" type="button" class="game btn btn-outline-secondary btn-lg" value="h">h</button>
                <button id="j" type="button" class="game btn btn-outline-secondary btn-lg" value="j">j</button>
                <button id="k" type="button" class="game btn btn-outline-secondary btn-lg" value="k">k</button>
                <button id="l" type="button" class="game btn btn-outline-secondary btn-lg" value="l">l</button>
            </div>
            <div class="keyboard_row">
                <button id="enter" type="button" class="game btn btn-outline-secondary btn-lg" value="Enter">ENTER</button>
                <button id="z" type="button" class="game btn btn-outline-secondary btn-lg" value="z">z</button>
                <button id="x" type="button" class="game btn btn-outline-secondary btn-lg" value="x">x</button>
                <button id="c" type="button" class="game btn btn-outline-secondary btn-lg" value="c">c</button>
                <button id="v" type="button" class="game btn btn-outline-secondary btn-lg" value="v">v</button>
                <button id="b" type="button" class="game btn btn-outline-secondary btn-lg" value="b">b</button>
                <button id="n" type="button" class="game btn btn-outline-secondary btn-lg" value="n">n</button>
                <button id="m" type="button" class="game btn btn-outline-secondary btn-lg" value="m">m</button>
                <button id="back" type="button" class="game btn btn-outline-secondary btn-lg"  value="Back">
                    {{!-- Following Code was taken from Bootstrap --}}
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-backspace" viewBox="0 0 16 16">
                        <path d="M5.83 5.146a.5.5 0 0 0 0 .708L7.975 8l-2.147 2.146a.5.5 0 0 0 .707.708l2.147-2.147 2.146 2.147a.5.5 0 0 0 .707-.708L9.39 8l2.146-2.146a.5.5 0 0 0-.707-.708L8.683 7.293 6.536 5.146a.5.5 0 0 0-.707 0z"/>
                        <path d="M13.683 1a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2h-7.08a2 2 0 0 1-1.519-.698L.241 8.65a1 1 0 0 1 0-1.302L5.084 1.7A2 2 0 0 1 6.603 1zm-7.08 1a1 1 0 0 0-.76.35L1 8l4.844 5.65a1 1 0 0 0 .759.35h7.08a1 1 0 0 0 1-1V3a1 1 0 0 0-1-1z"/>
                    </svg>
                </button>
            </div>
        </div>
    </div>

    <div class="col-5" style="border: 2px none black;">
        <div class="semicircle"></div>
        <div class="circle"></div>
        <div class="rectangle"></div>
        <div class="triangle-right"></div>
        <div class="vl"></div>
        <div class="wrapper">
            <div class="ball"> 
                <div class="texture"></div>  
            </div>
        </div>
        {{!-- From w3schools --}}
        <canvas id="myCanvas" width="300" height="750">
            Sorry, your browser does not support canvas.
        </canvas>
        <script>
        const canvas = document.getElementById("myCanvas");
        const ctx = canvas.getContext("2d");
        ctx.beginPath();
        ctx.moveTo(0, 425);
        ctx.quadraticCurveTo(190, 15, 30, 0);
        ctx.stroke();
        </script>

        <h1 id="golf-logo" class="top-center">WORD PAR</h1>
    </div>

    <div id="top-right-game" class="col-12">
        <form method="POST" action="/logout">
            <button id="exit-button" type="submit" class="btn btn-danger" style="background-color: green; color:white">Exit Game</button>
        </form>
    </div>
</div>

<script>
// ========================================
// AI Integration Variables
// ========================================
let secretWord = '';
let category = 'music'; // Default category
let guessHistory = [];
let attempts = 0;
const MAX_ATTEMPTS = 10;
let currentGameID = null;

// ========================================
// Initialize Game - Generate Secret Word
// ========================================
async function startNewGame(selectedCategory) {
    category = selectedCategory || 'music';
    attempts = 0;
    guessHistory = [];
    currentGameID = null;
    
    try {
        // Call AI to generate secret word
        const response = await fetch(`/api/generate_word?category=${category}`);
        const data = await response.json();
        
        secretWord = data.word;
        console.log('🎯 Secret word generated:', secretWord); // TODO: Remove in production!
        
        // Show feedback
        alert(`New game started! Category: ${category.toUpperCase()}`);
        
        // Clear previous guesses
        const existingHistory = document.getElementById('guess-history');
        if (existingHistory) {
            existingHistory.remove();
        }
        
    } catch (error) {
        console.error('Error generating word:', error);
        alert('Failed to start game. Please try again.');
    }
}

// Call this when page loads
window.addEventListener('DOMContentLoaded', () => {
    startNewGame('music'); // Start with music category
});

// ========================================
// Keyboard Input Handling
// ========================================
const buttonContainer = document.getElementById('buttonContainer');
let prevKey = "";
let buttonInputs = [];
let room = true;
    // See if the clear button is active
    let clear = false;

    // Update the user's guess on the screen
    function updateGuess(buttonInputs) {
        // Your chosen JavaScript code from above would go here
        // For example, using forEach():
        const guessDiv = document.getElementById("guess");

function updateGuess(buttonInputs) {
    const guessDiv = document.getElementById("guess");
    let htmlContent = "<div id='letters'>";
    buttonInputs.forEach(item => {
        htmlContent += "<div class='character rounded'>";
        htmlContent += `${item}`;
        htmlContent += "</div>";
    });
    htmlContent += "</div>";
    guessDiv.innerHTML = htmlContent;

    const clearbtnDiv = document.getElementById("clearbtn");
    let htmlContent2 = "<button id='clear' type='button' class='game btn btn-outline-secondary btn-lg' value='Clear'>Clear</button>"
    clearbtnDiv.innerHTML = htmlContent2;
    clearbtnDiv.addEventListener('click', buttonPressed);
}
        guessDiv.innerHTML = htmlContent;

        if (clear) {
            const clearbtnDiv = document.getElementById("clearbtn");
            let htmlContent2 = "<button id='clear' type='button' class='game btn btn-outline-secondary btn-lg' value='Clear'>Clear</button>"
            clearbtnDiv.innerHTML = htmlContent2;

        // Call the button function every time the clear is pressed (from the virtual keyboard)
        clearbtnDiv.addEventListener('click', buttonPressed);
        }
    }

async function buttonPressed(event) {
    if (room) {
        if (prevKey) {
            prevKey.classList.remove('simulate-btn-hover');
        }
        
        const correctKey = document.querySelector(`#${event.target.id}`)
        prevKey = correctKey
        if (correctKey) {
            correctKey.classList.add('simulate-btn-hover');
        }

        if (event.target.tagName === 'BUTTON') {
            if(event.target.id == 'enter') {
                if (buttonInputs.length === 0) {
                    alert('Please enter a word first!');
                    return;
            if (event.target.id === 'clear') {
                clear = true;
            }

            // console.log(event.target)
            if (event.target.tagName === 'BUTTON') {
                if(event.target.id == 'enter') {
                //    console.log('im here')
                clear = true;
                updateGuess(buttonInputs);
                    room = false;
                //    console.log(room)
                }
                else if (event.target.id === 'back') {
                    buttonInputs.pop();
                    updateGuess(buttonInputs);
                }
                else {
                    // Don't append the clear button
                    if (event.target.id != 'clear') {
                        buttonInputs.push(event.target.value);
                    }
                    // console.log(event.target.id)
                    updateGuess(buttonInputs);
                    // console.log(buttonInputs);
                }
                
                // Submit guess - this handles both AI check AND database save
                await submitGuess(buttonInputs.join(''));
                updateGuess(buttonInputs);
                room = false;
            }
            else if (event.target.id === 'back') {
                buttonInputs.pop();
                updateGuess(buttonInputs);
            }
            else if (event.target.id !== 'clear') {
                buttonInputs.push(event.target.value);
                updateGuess(buttonInputs);
            }
        }
        else {
            if (event.target.tagName === 'BUTTON') {
                 if (event.target.id === 'clear') {
                    // console.log(buttonInputs);
                    buttonInputs.length = 0;
                    // console.log(buttonInputs);
                    const guessDiv = document.getElementById("letters");
                    const clearbtn = document.getElementById("clear");
                    if (guessDiv) {
                        guessDiv.remove();
                    }
                    if (clearbtn) {
                        clearbtn.remove();
                    }
                    room = true;
                    clear = false;
                }
            }
        }
    }
}

// ========================================
// Submit Guess - AI Similarity + Database Save
// ========================================
async function submitGuess(userGuess) {
    attempts++;
    
    try {
        // 1. Save guess to database first
        const dbResponse = await fetch("/api/submitGuess", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
                userInput: userGuess,
                gameID: currentGameID || null
            })
        });

        const dbData = await dbResponse.json();
        
        if (dbData.success) {
            currentGameID = dbData.gameID;
            console.log("Guess saved to database, gameID:", currentGameID);
        }

        // 2. Calculate similarity with AI
        const aiResponse = await fetch('/api/similarity', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
                word1: userGuess.toLowerCase(),
                word2: secretWord.toLowerCase()
            })
        });
        
        const aiData = await aiResponse.json();
        const similarity = aiData.similarity;
        const percentage = Math.round(similarity * 100);
        
        // Store guess in history
        guessHistory.push({
            word: userGuess,
            similarity: similarity,
            percentage: percentage,
            attempt: attempts
        });
        
        // Display feedback
        displayFeedback(userGuess, similarity, percentage);
        
        // Check if won
        if (similarity >= 0.95 || userGuess.toLowerCase() === secretWord.toLowerCase()) {
            setTimeout(() => {
                alert(`🎉 YOU WON! The word was "${secretWord}"!\nAttempts: ${attempts}`);
                if (confirm('Play again?')) {
                    location.reload();
                }
            }, 500);
        }
        else if (attempts >= MAX_ATTEMPTS) {
            setTimeout(() => {
                alert(`😢 Game Over! The word was "${secretWord}".\nBetter luck next time!`);
                if (confirm('Try again?')) {
                    location.reload();
                }
            }, 500);
        }
        
    } catch (error) {
        console.error('Error processing guess:', error);
        alert('Error checking your guess. Please try again.');
    }
}

// ========================================
// Display Feedback with Visual Indicators
// ========================================
function displayFeedback(guess, similarity, percentage) {
    // Get or create feedback container
    let feedbackContainer = document.getElementById('guess-history');
    if (!feedbackContainer) {
        feedbackContainer = document.createElement('div');
        feedbackContainer.id = 'guess-history';
        feedbackContainer.style.cssText = `
            margin-top: 20px;
            padding: 15px;
            background: rgba(255,255,255,0.1);
            border-radius: 10px;
            max-height: 300px;
            overflow-y: auto;
        `;
        document.getElementById('top-left-game').appendChild(feedbackContainer);
    }
    
    // Determine feedback message and color
    let message = '';
    let color = '';
    
    if (similarity > 0.9) {
        message = '🔥 ALMOST THERE!';
        color = '#ff4444';
    } else if (similarity > 0.7) {
        message = '🌟 VERY CLOSE!';
        color = '#ff8844';
    } else if (similarity > 0.5) {
        message = '😊 GETTING WARMER!';
        color = '#ffaa44';
    } else if (similarity > 0.3) {
        message = '🤔 ON THE RIGHT TRACK...';
        color = '#88aaff';
    } else {
        message = '❄️ COLD!';
        color = '#4488ff';
    }
    
    // Create feedback element
    const feedbackItem = document.createElement('div');
    feedbackItem.style.cssText = `
        margin-bottom: 10px;
        padding: 10px;
        background: ${color};
        color: white;
        border-radius: 5px;
        font-weight: bold;
    `;
    feedbackItem.innerHTML = `
        <div style="display: flex; justify-content: space-between;">
            <span>${attempts}. ${guess.toUpperCase()}</span>
            <span>${percentage}%</span>
        </div>
        <div style="font-size: 0.9em; margin-top: 5px;">${message}</div>
    `;
    
    // Add to top of history
    feedbackContainer.insertBefore(feedbackItem, feedbackContainer.firstChild);
}

// ========================================
// Event Listeners
// ========================================
buttonContainer.addEventListener('click', buttonPressed);

document.addEventListener("keydown", function(event) { 
    if ((event.key.length === 1 && event.key.match(/[a-z]/i)) || event.key == 'Enter' || event.key == 'Backspace') {
        event.preventDefault();
        let keyTap;

        if (room) {
            if (event.key == 'Enter') {
                keyTap = { target: {tagName: 'BUTTON', value: event.key, id: 'enter'} };
            }
            else if (event.key == 'Backspace') {
                keyTap = { target: {tagName: 'BUTTON', value: event.key, id: 'back'} };
            }
            else {
                keyTap = { target: {tagName: 'BUTTON', value: event.key, id: event.key} };
            }
        }
        else {
            if (event.key === 'Backspace') {
                keyTap = { target: {tagName: 'BUTTON', value: event.key, id: 'clear'} }
            }
        }
        buttonPressed(keyTap);
    }
});
</script>