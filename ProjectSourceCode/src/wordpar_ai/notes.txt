
about Docker.File

context: ./wordpar_ai → tells Docker where to find the Dockerfile.

volumes: ./wordpar_ai:/app → mounts your code so you can edit locally and see changes in the container.

depends_on: - db → ensures the AI service waits for the database if needed.

.venv is just a bunch of .py library and folders that i downloaded

old docker file packages were too large
FROM python:3.13-slim

WORKDIR /app

COPY . .

RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 5000

CMD ["python", "ai_service.py"]